# Project Memory Bank

Checkpoint written 2026-09-04 (signed off by jyk2000). Read this instead of
replaying the session.

## OPERATING RULE: DO NOT RAPID-POLL LONG RUNNING COMMANDS

Wait for a long running container (replay, full test suite) with ONE blocking
`while` loop that holds the terminal until the container exits. NEVER fire a
series of short `sleep`+`docker inspect` polls one after another -- that makes
"Command was aborted" errors. The correct pattern:

    while docker inspect -f '{{.State.Running}}' <CONTAINER> 2>/dev/null | grep -q true; do sleep 8; done; \
      docker inspect -f '{{.State.Status}} rc={{.State.ExitCode}}' <CONTAINER> 2>&1; \
      docker logs <CONTAINER> 2>&1 | grep -E 'passed|failed' | tail -2

Launch it detached, then run the loop ONCE and wait for it to return. Also:
DOMINIQUE WALLS + RODERICK MCBRIDE are NOT in the JSON dump -- ignore their 0%
rows entirely.

## CHECKPOINT 2026-09-07 (LATEST): Young Teak Kong empty-reposition gaps closed

Committed as `de82976` (code/tests). Replay trajectory for 08/28:

| run | mode | score |
|---|---|---|
| replay8 (previous checkpoint) | photos-on | 93/120 = 78% |
| replay9 (parser/state fixes) | text-only | 96/120 = 80% |
| replay10 (+ empty-drop inference) | text-only | 98/120 = 82% |

YOUNG TEAK KONG went 4/8 -> 8/8 (100%). HYUNGBAE KIM also picked up a match
(9/10). Full `test_state_machine.py`: **123 passed**.

### What changed

1. **CASE 2 load-pickup promotion** (`state_machine.py` 0b): if a pickup was
   filed as an arrival but the caption names a destination ("Load pick up
   D 020 sds to 200 Kong"), the two-facility recovery now promotes it to a
   CASE 1 departure. A pickup with no destination still reaches the
   no-destination card.
2. **CASE_WORK_FINISHED LLM-pair fallback** (`state_machine.py` 0b): when the
   raw-text regex cannot resolve a bare site alias ("finish Live unloading e2
   to sds" -- E2 is a site, not a location code), use the LLM's already-parsed
   canonical origin/destination pair instead of dropping the departure.
3. **Finish-unload origin correction** (`leg_cases.py` CASE 1): after a live
   unload the driver is at the delivered site. If the empty-reposition caption
   contains a stray facility name ("empty 200 to sds" after unloading at E2F),
   use the just-unloaded leg's destination as the origin.
4. **Empty-drop inference** (`leg_cases.py` CASE 2 + `leg_context.py`): when an
   EMPTY drop arrives at a facility with no open trip, infer the silent empty
   reposition from the last completed non-positioning leg's destination.
   Backdate the departure to that leg's `finished_time` (Young Teak Kong
   15:19 drop -> the manual log's 14:40 200F->SDS row) and immediately close
   the reconstructed leg at the drop time. Guarded to same-day/cross-site and
   no already-recorded move in the window.

### Still open

- JOHN SHIM E2F->E2R 10:17 (positioning) and the E1/SDS bobtail/empty legs.
- HYUNGBAE KIM 11:32/15:40 200F->7634 Load Pick Up rows.
- ILPYO HONG clock-in/yard rows.
- Walls + McBride roster gap (14 rows).
- Full-suite conftest truncate hang (pre-existing).

## CHECKPOINT 2026-09-07 (previous LATEST): receiver-POD backfill refined after photos-on replay -- SUPERSEDED above

Committed as `b95d5e5` (code/tests/compose/helpers). This memory checkpoint is
the second commit on top. The older "CLI handoff" section immediately below is
superseded by it -- keep this as the current state.

### Final code behaviour (`bot/leg_cases.py` `handle_case_1_departure`)

A duplicate BOL posted with a receiver signature backfills the ORIGINAL leg
(`bol_image`, `document_type`, `paperwork_time`, `receiver_signed=1`). The early
return is ONLY taken when the caption is a document-only/same-route repost. If
the caption also announces an onward movement (e.g. "finish live unloading empty
7634 to 200"), the original leg is POD-backfilled AND the new empty-reposition
leg is still opened -- movement wins; the old BOL fields are discarded from the
new leg and no Stale-BOL card is raised.

Why: the naive first version returned `{leg_id: duplicate_of}` for every
receiver-signed duplicate. In a photos-on replay that swallowed the onward
movement on exactly those finish-unload captions (the POD photo is attached to
the same message as the empty reposition), dropping the full-day score to
78/120 = 65%.

### Tests (`tests/test_state_machine.py`)

- `test_reposted_receiver_signed_pod_backfills_the_original_leg` (pure repost)
- `test_reposted_shipper_signed_duplicate_is_still_stale`
- `test_receiver_signed_pod_with_onward_movement_still_records_the_leg` (NEW)
Targeted: **5 passed**. Full `test_state_machine.py`: **119 passed**.

### Helper scripts (`tools/`, executable, LF)

`run_tests.sh` / `run_replay.sh` rewritten to:
- pass `-k 'multi word filter'` through correctly (`printf %q` per arg);
- run `docker compose run` WITHOUT `--rm`, then `docker wait` + `docker logs`
  so the exit code and full output survive (older versions printed
  `error: no such object` after `--rm` deleted the container);
- replay output captures up to 300 lines, including the score table;
- result files: `backups/.test_result.txt`, `backups/.replayN_result.txt`,
  plus `.replayN_full.log` copies of the complete run.

### Replay evidence (08/28, 120 manual rows)

| run | mode | score |
|---|---|---|
| replay5 (CLI handoff) | text-only | ~90/120 = 75% (log truncated) |
| replay6 | photos-on BEFORE refinement | 78/120 = 65% ← bug caught |
| replay7 | text-only same tree | 91/120 = 76% |
| replay8 | photos-on AFTER refinement | 93/120 = 78% (best photos-on) |

replay8 per-driver: JUNGGEUN KIM / KISOO HAN / MATTHEW CHO / SOKHWAN YUN /
YUNGEUN LEE 100%, HYUNGBAE KIM 90%, JOSEPH KIM 100%, YOUNGPYO KIM 91%.
Remaining misses are mostly the Walls/McBride roster gap (14 rows), JOHN SHIM's
E1 legs, ILPYO HONG clock-in/yard rows, and YOUNG TEAK KONG's origin inference.

### Other working-tree changes

- `docker-compose.yml`: host MySQL port is `${MYSQL_HOST_PORT:-3306}:3306`.
- Stray empty root file `26.2.1` deleted.
- Open issues unchanged: full-suite conftest truncate hang (use
  `test_state_machine.py` / targeted runs), Walls+McBride roster gap, photos-on
  vision cost/nondeterminism.

## CHECKPOINT 2026-09-07 (CLI handoff) -- SUPERSEDED by LATEST above

Latest session was in Cline (UI mode) on 2026-09-07. Everything below is
**UNCOMMITTED in the working tree** (HEAD still `bc42582`). Repo state:

    git status: M .memory.md  M bot/leg_cases.py  M docker-compose.yml  M tests/test_state_machine.py
                ?? tools/run_tests.sh  tools/run_replay.sh  ?? 26.2.1 (stray empty file, safe to delete)
    docker ps   -> only freight_bot-mysql_db-1 Up (healthy), 0.0.0.0:3306->3306/tcp

### The code change: receiver-signed POD repost backfills the ORIGINAL leg

Problem (seen in replay5, driver #6786826579 / BOL 926761 / Leg #72 style logs):
when a driver reposts the SAME BOL with a receiver stamp/signature (proof of
delivery), `handle_case_1_departure` treated it as a STALE BOL -- discarded the
image, opened a new leg without paperwork, and never marked the original leg
receiver-signed. The POD was lost.

Fix in `bot/leg_cases.py` `handle_case_1_departure` (after
`find_duplicate_bol_leg()`): if `duplicate_of and receiver_signed`, run
`UPDATE shuttle_legs SET bol_image=COALESCE(...), document_type=..., paperwork_time=..., receiver_signed=1 WHERE id=duplicate_of`, commit, return `{is_clean:True, leg_id:duplicate_of}` -- NO new leg is opened, and it does not matter whether the old leg is COMPLETED or in transit. A shipper-signed duplicate still takes the stale path (unchanged).

Tests added in `tests/test_state_machine.py` (near the existing stale-BOL tests):
- `test_reposted_receiver_signed_pod_backfills_the_original_leg` - original COMPLETED leg with bol_image + receiver_signed=0; repost same BOL receiver_signed=True -> leg_id==original, bol_image==IMG, receiver_signed==1, document_type, paperwork_time set, only 1 leg total.
- `test_reposted_shipper_signed_duplicate_is_still_stale` - shipper_signed=True duplicate still returns Stale BOL card, 2 legs, new in-transit leg has no bol fields.

Result: targeted run passed **4 passed** (`-k 'reposted or stale_bol'`) in ~15s.
Pytest args in the container: `python -m pytest tests/test_state_machine.py -k 'reposted or stale_bol' -q`.

### OPEN ISSUE: full test suite fails to finish (pre-existing, NOT ours)

The full suite **hangs at 37%** on a `TRUNCATE TABLE ...` in the conftest
fixture (schema rebuild). Reproduced even after a clean MySQL restart and after
fresh `DROP DATABASE shuttle_db_test; CREATE DATABASE` -- so it is an
environmental MySQL 8.0.46 metadata-lock issue in `tests/conftest.py`'s teardown
(`waiting for handler commit` / `checking permissions` state on the truncate),
NOT introduced by the BOL change. Recorded so it does not consume the next
session: do not loop restarting containers over it; the *targeted* tests are
what verify this change until the suite teardown is understood/fixed.

### New helper scripts (tools/, executable, LF)

- `tools/run_tests.sh`   - runs tests in BACKGROUND (timeout-proof vs the UI's
  30s tool abort), best via: `tools/run_tests.sh -k 'reposted'` or bare for all.
  Writes result to `backups/.test_result.txt`.
- `tools/run_replay.sh`  - same for replay: `REPLAY_PHOTOS=1 tools/run_replay.sh` (etc).
  Writes score to `backups/.replay<N>_result.txt` (auto-increments).

Both wrap the docker compose commands, and follow the operating rule (single
blocking `while docker inspect` wait; never rapid-poll). They accept a
`-k`/`-f` filter or plain pytest args for the tests script.

### Replay status / data

- `.env` now has `REPLAY_PHOTOS=1`, `REPLAY_DEBUG=1` (user saved it mid-session;
  an earlier run printed `photos=off` because the file had not been saved).
  DO NOT rely on `.env` when composing manually -- the replay reads `REPLAY_*`
  from `.env` via docker-compose.
- Last full replay result: `backups/.replay5_result.txt` (90 matched /120 = 75%,
  text-only, `photos=off`). Walls/McBride still 0% (roster gap, 14 rows).
- With photos+debug now enabled, the NEXT run is expected to do the vision pass
  (fills `bol_image` / trailer / BOL #s) and should score notably better.
- Prior trajectory for 08/28: 88/120 (73%) -> 91 (76%) -> 95 (79%) -> replay4 96 (80%).
- A next replay is worth running to confirm the receiver-POD backfill also reduces
  the "already recorded / saving movement without paperwork" count.

### MySQL / port bindings (local dev)

- The project DB is the compose service `mysql_db` (image mysql:8.0), data on a
  named Docker volume `freight_bot_mysql_test_data` (from the test overlay).
- Host port: `docker-compose.yml` port mapping is now
  `- "${MYSQL_HOST_PORT:-3306}:3306"` (was a plain `3306:3306`). Default 3306.
  Earlier a stale Windows-side mysqld (part of Docker Desktop's `com.docker.backend`
  port-forward) held host 3306 and forced a temp `MYSQL_HOST_PORT=3307`; that
  override was removed from `.env` once the conflict cleared, so it's back to 3306.
- If host 3306 is ever occupied again: set `MYSQL_HOST_PORT=3307` in `.env`;
  replay/tests still connect over the internal `mysql_db:3306` name, unaffected.
- The test/replay services reach MySQL by **service name** `mysql_db:3306` over
  the compose network -- host ports are irrelevant to them.
- To inspect from a host tool like DataGrip, point at the **container bridge IP**
  (e.g. `172.18.0.2:3306`) or container name `mysql_db`, NOT the Windows
  Docker-Desktop port-forward. The "wrong IP db" confusion (saw 09/04 legs in
  DataGrip) was because DataGrip had hit Docker Desktop's 3306, not this container.

### Useful re-run commands (CLI/terminal)

```bash
# named tests (fast, verifies the BOL fix)
tools/run_tests.sh -k 'reposted or stale_bol'

# full suite (be patient; see OPEN ISSUE above re: the 37% truncate hang)
tools/run_tests.sh

# replay (cheap single-driver debug pass, or full with photos. Takes a while.)
REPLAY_DRIVER="KISOO HAN" REPLAY_PHOTOS=1 tools/run_replay.sh
REPLAY_DEBUG=1 tools/run_replay.sh

# inspect legs in the scratch DB (bol_image / receiver_signed live here)
docker compose -f docker-compose.yml -f docker-compose.test.yml exec mysql_db mysql -uroot -p -D replay_scratch
SELECT id, bol_number, receiver_signed, (bol_image IS NOT NULL) has_img, destination_location, leg_status FROM shuttle_legs ORDER BY id;
```

## CHECKPOINT 2026-09-04: ETA + state_machine SPLIT (committed & pushed)

Everything committed and pushed to `origin/main` (HEAD `2ebf7c4`); working
tree clean; only `mysql_db` running. Full test suite: **165 passed** against the
local Docker MySQL (test_state_machine, test_load_types, test_shifts,
test_dwell); stable across the whole refactor.

### What changed in `2ebf7c4`
- **ETA per leg** (new `bot/eta.py`): `drive_minutes` reads drive time from
  `location_distances` (weighted_minutes preferred, drive_minutes fallback;
  tries raw pair then alias-normalized pair); `lunch_overlap_minutes` caps at
  60 while in progress, real elapsed when ended. `shuttle_legs` gained
  `eta_minutes`, `lunch_start`, `lunch_end` (schema_ddl additive columns);
  departures store `eta` via `drive_minutes`. ai_engine prompt now teaches
  drops-as-arrivals ("empty dropped do 20 sds" = CASE_2 arrival) and
  `FACILITY #NN` = facility + dock (number never a trailer).
- **state_machine split** (was one 1705-line function `commit_trip_leg`):
  - `bot/leg_cases.py`  (~1475) — one `async handle_*(ctx)` per case type
    (lunch/clock/work_finished, CASE_1 departure, CASE_2 arrival,
    CASE_HISTORICAL_BOL_UPDATE, CASE_AUTO_RESOLVE, CASE_3 intra-move,
    CASE_MANIFEST, PARSE_FAILED, default).
  - `bot/leg_context.py` (~330) — `LegContext` (parsed intent + conn/cur +
    `display_trailer`) and the old shared helpers now methods
    (`find_duplicate_bol_leg`, `resolve_round`, `current_round_number`,
    `stamp_finished`, `open_departure_leg`, `start_hooked_load`,
    `pickup_with_no_destination`).
  - `bot/leg_helpers.py` (~192) — regexes (BOBTAIL/PICKUP/UNLOAD/LOAD/
    MOVEMENT_CUE/DEPARTURE_TO patterns) + `cross_facility_destination`,
    `two_facilities_in_order`, `facility_dock_from_text`, `not_a_facility`.
  - `bot/state_machine.py` (~256) — thin orchestrator. `commit_trip_leg`
    signature UNCHANGED (`from state_machine import commit_trip_leg` still
    works for handlers/conftest/replay). Reads intent -> LegContext -> resolves
    load status -> driver verify -> pre-match reclassification (0b two-facility,
    0c origin inference, 1 same-site demote, 1b yard->departure promote) ->
    lunch -> dispatch via `CASE_HANDLERS[case_type]`.
- **Tools** (dev-only, not imported by bot): `tools/gen_cases.py` (split
  generator), `tools/verify_split.py` (SQL/UI-string multiset diff vs pre-split
  original), `tools/verify_helpers_ast.py` + `tools/verify_context_ast.py`
  (AST-equivalence: all helper functions and context methods identical to the
  original monolith's, modulo self->attr + SQL-whitespace normalization).

### Notable bugs hit & fixed during the split
- `two_facilities_in_order` missing trailing `return None, None` -> returned
  bare `None`, breaking unpacking in `commit_trip_leg` (caught by
  `test_standalone_finish_stamps_the_current_leg`; fixed).
- Added missing `from rm_manifest import ... record_rm_load` to leg_cases;
  stale-BOL clears in handle_case_1 now write `X = ctx.X = None` so
  `open_departure_leg` sees cleared paperwork.

### Original pre-split survivor
- `bot/pre_split_state_machine.py` (the 1705-line old file) was DELETED.

### Replay / next session
- Full replay run planned to re-check against a real day after the split.
- NOTE: if the replay DB predates the schema change, start the bot once (or
  run `apply_schema`) so the additive `eta_minutes`/`lunch_start`/`lunch_end`
  columns exist before replaying; otherwise INSERTs referencing `eta_minutes`
  fail.
- Prior replay scores (08/28 trajectory): baseline 88 (73%) -> replay-full
  91 (76%) -> replay-final3 95 (79%) -> replay4 96 (80%).

## RESUME HERE (next session)

**IN PROGRESS replay is COMPLETE — `replay4` finished Fri Sep 04 01:48:55 with
rc=0: 96/120 (80%)**, the best yet (was 95/120 @ 79%). Full table + missing/extra
rows in `backups/.replay4_result.txt` (gitignored, dot-file on disk only).

Everything is committed and pushed (HEAD `1719b8e`); working tree clean; only
`mysql_db` running. Replay scores for 08/28 (trajectory): baseline 88 (73%) →
`replay-full-final` 91 (76%) → `replay-final3` 95 (79%) → **`replay4` 96 (80%)**.

Per-driver (replay4): 100% = JUNGGEUN KIM / KISOO HAN (was 71%) / MATTHEW CHO /
SOKHWAN YUN / YUNGEUN LEE; JOSEPH KIM 92%; YOUNGPYO KIM 91%; JOHN SHIM 86%;
HYUNGBAE KIM 80%; ILPYO HONG 78%; YOUNG TEAK KONG 75%; DOMINIQUE WALLS +
RODERICK MCBRIDE 0% (still the roster gap, 14 rows of the 24 misses).

New/notable from replay4 (not yet fixed):
- JOSEPH KIM 200F→E2F 08:35 LOAD PICK UP lost; bot made an origin-UNKNOWN
  "SPOT" leg at 08:35 (round=1), so the matcher can't confirm it.
- New phantoms: ILPYO HONG 7634→200F 15:53 (round=2), MATTHEW CHO SDS→SDS
  14:55 (round=5 R2), plus the standing MATTHEW CHO 200F→200F 13:46 same-site.
- Known-good context in `4c85597` (phantom-departure stop + name-prefixed
  parse) and `c08b4af` (bobtail/empty/typo + to-dropped); 116/116 state_machine
  tests pass.

## RESUME (prior context)

Everything below is committed and pushed (HEAD `c08b4af`); working tree clean;
only `mysql_db` running (healthy). The three earlier fixes are IN `c08b4af`:

**Full photos-on replay of 08/28 is COMPLETE** (all drivers, `replay-full-final`,
rc=0): **93 legs produced, 91/120 matched (76%)** — up from 88/120 (73%)
baseline. Per-driver: JOSEPH KIM / JUNGGEUN KIM / SOKHWAN YUN / YUNGEUN LEE
100%; YOUNGPYO KIM 91%; JOHN SHIM 86% (was 57%); KISOO HAN 71%; HYUNGBAE KIM
70%; MATTHEW CHO 75-88% (LLM nondeterminism: 09:47 "Empty 200 sds" misses when
parser files CASE_WORK_FINISHED); ILPYO HONG 67%; DOMINIQUE WALLS + RODERICK
MCBRIDE 0% (roster gap). Legs + RM loads (`rm_loads`) live in `replay_scratch`.

**CONTINUING WORK (uncommitted in working tree):** parse-robustness fix for
the "to"-dropped departure ("Empty 200 sds" = 200→sds). Two parts:
1. `ai_engine.py` prompt now teaches: two adjacent KNOWN VALID CODES without
   "to" are a departure, first code origin, second destination.
2. `state_machine.py` `_two_facilities_in_order()` recovers a two-facility move
   from a CASE_WORK_FINISHED/NONE_WORK_RELATED/CASE_2 message (before the match),
   and GUARD 1 no longer requires a trailer for EMPTY moves (only LOADED).
New test `test_work_finished_with_two_facilities_no_to_records_a_departure`.
Full `test_state_machine.py` suite running (should be 115+ passed).

**JOHN SHIM improved 57% (8/14) → 86% (12/14) on the matcher, and 14/14 legs
are now ACTUALLY produced.** The 2 matcher "NOT PRODUCED" rows are intentional
artifacts: `tools/replay.py` line 141 excludes `is_positioning_leg = 1` from
`bot_legs`, so yard moves don't count. The manual rows they map to:
- "E2F→E2R 10:17" IS produced as a positioning leg (leg 2) but excluded.
- "E1→SDS 14:15 Bobtail" IS produced (leg 12) but the greedy matcher consumed
  it for the unknowable 13:15 "Lunch off" row.

The fixes in `bot/state_machine.py` (uncommitted) that took John Shim's catch to
complete:
1. GUARD 1 no longer rejects a bobtail for missing trailer.
2. A CASE 3 yard-move message naming a CROSS-FACILITY "to X" is recorded as a
   departure (drop half completes prior leg's arrival).
3. A work_finished E2-only same-code move ("e2r g ... to e2r") recovers the
   origin from the last completed trip leg's destination -> E2F→E2R, matching
   the manual (driver typo'd the origin). **Only fires for site E2** so
   200F→200R yard-move folding is untouched. Uses `dest_loc=origin_loc` then
   `origin_loc=completed_dest`, with `from_facility` preferring `origin_loc`
   and `to_facility` falling back to `facility` (the destination).

Tests added: `test_bobtail_with_no_trailer_still_records_a_leg`,
`test_yard_move_with_cross_facility_departure_becomes_a_leg`,
`test_yard_move_to_a_dock_stays_a_yard_move`,
`test_work_finished_move_with_typo_origin_uses_last_trip_dest`.
Full `test_state_machine.py` suite: 113 passed, 1 failed (the typo test -- fix
in progress: to-side must keep the destination text facility).

Everything below is committed and pushed (HEAD `fd2ecce`); only `mysql_db` is
running (healthy). The three earlier fixes are IN `fd2ecce`:
1. CASE 3 dock-move with work_finished now completes the prior trip leg
   (`stamp_finished` sets `leg_status='COMPLETED'`).
2. Round counters keyed to the departure's own day (`DATE(departure_time) =
   DATE(msg_timestamp)`), fixing "all rounds = 1".
3. `load_types.classify()` matches exact facility lanes first, then a guarded
   site-pair fallback, fixing "load_type = NULL on every leg". `trip_seq`
   column dropped (idempotent migration in `schema_ddl.apply_schema`).

`replay_scratch` currently holds the last TEXT-ONLY run: 8 legs, rounds
1,1/2,2/3,3/4, LOADED legs typed `FG STO`. Leg 8 of the full day (SDS→200F
14:24, finished 15:28:40) is MISSING from that run because text-only cannot
read the BOL SHIP TO ("Load trailer pickup sds dock28 #77209"). Only a
photos-on replay (`REPLAY_PHOTOS=1`) materialises it -- run that if the user
wants the authoritative 9-leg + load_type check (costs Gemini API calls).

Ops notes: `docker compose ... run` must be detached (`run -d --name X`, poll
`docker inspect -f '{{.State.Status}} rc={{.State.ExitCode}}' X`); foreground
hangs the exec, and rapid 25s poll-chains cause aborts -- wait real intervals.
`docker rm -f X` the named container after. Only `mysql_db` is up locally;
`telegram_bot` and `db_backup` are not running (`docker compose up -d --build`
brings the full stack).

## Current Status

Telegram dispatch bot for short-haul shuttle work, run for **SFL** whose client
is **Qcell** (warehouse **SDS**). A competitor still holds Eagle 3 and some
E1/E2 routes; SFL is winning them over by proving better and cheaper service.
The trip-leg records are the evidence for that, and go to Qcell's accounting.

- Branch **`main`**, HEAD `fd2ecce` (2026-09-01), **pushed** -- the working
  tree is clean. Repo is **private** (was public; old rotated credentials
  remain in history at `c551cbe`).
- Tests: full suite currently has a known pre-existing failure group in
  `tests/test_load_types.py` (10 old failures caused by the site-vs-facility
  lane lookup) that is now FIXED in the current code; the suite was NOT fully
  re-run end-to-end after `fd2ecce`, but `test_state_machine.py` (110 passed)
  and `test_load_types.py` (24 passed, 0 failed) pass in isolation. Latest
  count on a clean full run should be verified if a CI/full run is wanted.
- Not deployed/E.C2 unchanged; only local `mysql_db` runs.
- Company started shuttle work **2026-07-22**; nobody had prior shuttle
  experience. Processes are still forming, so treat "how we do it" as provisional.

## Running things

```bash
# tests (real MySQL, ~7 min)
#   foreground: docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm tests
#   background (timeout-proof) -> backups/.test_result.txt:
tools/run_tests.sh                      # full suite
tools/run_tests.sh -k 'reposted'        # single-area filter, any pytest args

# replay a real day against the dispatcher's hand-entered log
#   foreground: docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm replay
#   background (timeout-proof) -> backups/.replayN_result.txt:
tools/run_replay.sh
#   REPLAY_PHOTOS=1  also run vision (slow, costs API calls)
#   REPLAY_DRIVER="JOHN SHIM"  narrow to one driver
#   REPLAY_DEBUG=1   why each unmatched manual row was rejected
# legs stay in `replay_scratch` afterwards; the next run rebuilds it

# live parser checks (need GEMINI_API_KEY, not part of the suite)
tests/bench_live_parser.py  bench_live_shift_cases.py  bench_live_missed_messages.py
```

### How to replay a day

The replay feeds a real day of Telegram messages through the bot and compares
the legs it produces against the dispatcher's hand-entered log. It writes to
the scratch DB `replay_scratch` and never touches production.

**Prerequisites**
- `mysql_db` running (`docker compose up -d mysql_db` if not).
- `MYSQL_ROOT_PASSWORD` and (if photos are on) `GEMINI_API_KEY` — these read
  from `.env`.
- The fixture data is already in the repo: `data/ChatExport_2026-08-29/result.json`
  (the messages) and `data/SFL Log 08_2026.xlsx` (the dispatcher's truth),
  mounted read-only from `./data`.
- If the schema changed (e.g. new columns like `eta_minutes`), the replay builds
  its own DB with `apply_schema`, so no manual migration is needed — it starts
  clean every run.

**Run it**

```bash
# full day, text-only (default): fastest, no Gemini cost in container
docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm replay

# with photos on (reads BOL SHIP TO off attached photos — needed for some legs
# like dropless arrivals; SLOW and calls the Gemini API)
REPLAY_PHOTOS=1 docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm replay
```

**Environment options** (all optional; the compose overlay passes them through)
- `REPLAY_SHEET=08282026` — which day's sheet in the workbook is the truth.
- `REPLAY_PHOTOS=1` — run vision on photos (slow, costs API calls). Text-only
  misses any leg whose destination is only readable from the BOL photo.
- `REPLAY_DRIVER="JOHN SHIM"` — narrow to one driver to debug a single case.
- `REPLAY_DEBUG=1` — per-driver mismatch diagnostics: for every unmatched manual
  row, shows the closest bot candidate and *why* it was rejected. **Recommended.**

**Waiting for it (do NOT rapid-poll)**

Replay is a long-running container. Follow the operating rule at the top of
this file: launch it detached **once**, then hold the terminal with a single
blocking `while` loop, never a series of short `sleep`+`inspect` polls:

```bash
docker compose -f docker-compose.yml -f docker-compose.test.yml run -d \
  --name replay_run --rm replay
while docker inspect -f '{{.State.Running}}' replay_run 2>/dev/null | grep -q true; do sleep 8; done; \
  docker inspect -f '{{.State.Status}} rc={{.State.ExitCode}}' replay_run 2>&1; \
  docker logs replay_run 2>&1 | grep -E 'passed|failed|messages replayed|/120' | tail -5
```

**Reading the score**

The run prints `NN/120 (PP%)` — bot legs matched against the 120 manual rows.
Walls + McBride (14 rows) will always miss until the roster gap is fixed, so
their 0% is expected. Full missing/extra row details go to
`backups/.replayN_result.txt` (gitignored).

**Inspecting the produced legs**

The scratch DB is kept after a run (dropped at the start of the *next* run) so
you can read the exact legs the bot produced:

```bash
docker compose exec mysql_db mysql -uroot -p -D replay_scratch
SELECT * FROM shuttle_legs ORDER BY user_id, departure_time;
```

Docker is native `docker.io` inside WSL2 (systemd), **not** Docker Desktop.
MySQL cannot use a `/mnt/c` bind mount — InnoDB will not initialise on drvfs;
the test overlay uses a named volume.

## Architecture

`bot/` — `main.py` (startup, seeding, JobQueue), `config.py` (table names),
`schema_ddl.py` (**the only schema definition**), `ai_engine.py` (Gemini text +
vision, location cache), `handlers.py` (Telegram), `state_machine.py`
(`commit_trip_leg`, all cases), `routes.py`, `load_types.py`, `rm_manifest.py`,
`manifests.py`, `shifts.py`, `dwell.py`, `seed_network.py`.

`bot/data/` is operational data, edited not coded: `locations.csv`,
`docks.csv`, `drivers.csv`, `load_types.csv`, `location_distance.csv` (routes
are seeded from `routes.py`).

**There are TWO copies of every one of those CSVs** -- `bot/data/` and the
repo-root `data/` -- and both are tracked. **The code only ever reads
`bot/data/`** (`seed_network.DATA_DIR` and `load_types.CSV_PATH` are both
relative to `bot/`). Root `data/` also holds the Telegram export and the
dispatcher's workbook, which the replay needs and the bot does not. Edit
`bot/data/` or the change does nothing. This has already bitten once: see Open
items.

## Domain model

**Geography** is an I-75 corridor: Dalton north (E1, E2F, E2R, SDS, 210, 3551),
Adairsville middle (200F, 200R, 7634, 300), Cartersville south (100, 1380).
Truck repos are **200** and **SDS**. Client calls the plants Eagle 1/2/3; only
Eagle 2 has front/rear. `200F`/`200R` and `E2F`/`E2R` are **one site each**.

**E2F and E2R are the SAME building**, split by function: E2F is RM
inbound/outbound; E2R is RM inbound (IQC), FG inbound (OQC), FG outbound. A
move E2F→E2R with NO "empty" means the trailer is still LOADED (second half of
a two-part unload) — a real leg. An **empty E2F→E2R can happen but is handled
by Hanjin drivers, not SFL** — so SFL should not be recording empty E2F→E2R
moves as its own work. A `200F→200R` yard move normally *does* say "empty".
Drivers typo the facility (John Shim 10:17 08/28 wrote "e2r g ... to e2r"
meaning E2F→E2R); the bot can recover the origin from the last trip leg's
destination when a work_finished move has the same code on both ends.

**Seven defined routes** (`routes.py`, seeded to DB):
R1 `200F→E2F/E2R→SDS→200F`, R2 `200F⇄SDS`, R3 `SDS⇄200F`, R4 `SDS⇄7634`,
R5 `E1⇄210`, R6 `3551⇄E1/E2F/E2R`, R7 `200F⇄7634`.

**A round** is one traversal of a route: leave an anchor, work the stops
belonging to a route anchored there, return. Anything off-route is a **spot
delivery** forming its own round.

**Load type** comes from the lane, on LOADED legs only (`load_types.csv`).
`200→E2` RM, `E2→200` RM Inbound, `SDS→200` FG STO, `200→SDS` Spot Delivery,
`E1⇄210` FG E1, `200⇄7634` FG ES, `SDS⇄7634` FG DS, `3551⇄E1/E2` RM 3551.
OQC/IQC Recall stay with the dispatcher's dropdown — indistinguishable from RM.

**Doors are numbered in bands, and the band is the meaning** (`docks.csv`).
At 200 the front building runs **3-21 FG inbound** and **22-45 RM inbound**
(worked by **Hanjin**, not us); the rear runs **47-66 RM outbound** and
**67-99 FG outbound** (the 200⇄7634 lane). Upper bounds are approximate and 46
belongs to nobody. So a driver naming only a door has named a building: "#3 to
#47" is 200F→200R. After a live unload on an inbound door, drivers stage the
empty on a **vacant RM outbound door** so the next RM round -- theirs or
another driver's -- can hook and go. Only the 200 site has bands recorded.

**Dwell** (arrival → next departure) is the metric that differentiates, not
ETA; every operator meets transit times. Thresholds 20/30/40 min, escalating
once per level.

## Critical constraints — DO NOT OVERWRITE

- **`bot/schema_ddl.py` is the only schema.** `init.sql`/`schema.sql` were
  deleted for drifting. Never add a second copy. Test fixtures seed via the
  production path for the same reason — three hand-written copies drifted.
- **Round grouping is NOT distance-based.** Tried and failed: `E1→210` is short,
  and 100/1380 are *nearer* to 200 (15.5/19.1 mi) than every routine stop (21+).
  Cycle detection also fails. A test pins this.
- **`python-telegram-bot` 20.8 (httpx~=0.26) and `google-genai` 2.x
  (httpx>=0.28.1) are incompatible.** google-genai pinned to 1.2.0. Upgrading
  it needs PTB 22.x first. `[job-queue]` extra is required for dwell alerts.
- **Model is `gemini-flash-lite-latest`.** Verify model ids against ListModels,
  never from memory (`gemini-3.5-flash-lite` is real but flakier: 3/5 vs 5/5).
- **Gemini calls must stay in an executor**; they block and retry ~15s.
  `PARSE_POOL_WORKERS=32` because the default is 8 on a t3.xlarge.
- **A parse failure must never return `NONE_WORK_RELATED`** — that silently
  discards the movement. Use `PARSE_FAILED`, which raises a card.
- **Movement always wins over a completion or a lunch report.** Real messages
  carry both ("7634 unloading finished Empty to 200R"). Choosing the completion
  loses the trip. Same for lunch.
- **Never merge expected and reported columns.** The dispatcher owns expected,
  the bot owns reported; an inferred time in a reported column becomes
  indistinguishable from an observation. Applies to shifts, arrivals, manifests.
- **A duplicate BOL is a driver error, never an override.** Record the movement,
  discard the stale paperwork, card for a repost.
- **Timestamps come from Telegram**, including `forward_origin` for forwards.
  Never `datetime.now()`.
- **Facility codes are never docks or trailers.** Guards exist in both places.
- **A drop at the facility a trip is still running to is that trip's ARRIVAL**,
  not a yard move. The parser cannot tell -- only the open leg knows the driver
  was on their way there. Guarded to drops with no position moved FROM and no
  second place named, so "door 7 to yard" and "200F to 200R" stay moves.
- **A leg's `trailer_number` is often inherited, not observed.** CASE 1 copies
  it from the driver's previous leg when the message names none, so it goes
  stale at the first trailer swap. Never treat it as evidence; a trailer named
  at a drop is first-hand and wins.
- **PACTRA means 200, not 7634** — SDS bought the site from Pactra and long
  serving drivers still use the old name. 7634's official name is EPC PACTRA,
  which makes the wrong answer look right.

## Done 2026-08-30 (uncommitted, working tree)

Four defects found by replaying 08/28 against the dispatcher's log, all fixed
with tests. `state_machine.py` CASE 3 is where most of it lives.

1. **A drop at the destination created a second leg.** "Pickup empty pactra to
   sds" 07:32, then "Drop empty trailer at sds yard D021 #25773" 08:09 -- the
   drop was filed as an SDS→SDS yard move and the real leg stayed open until
   the next departure closed it at 08:20. Now it completes the open leg with
   the drop's time, dock, DO# and trailer. Sokhwan Yun's three SDS runs now
   match the dispatcher's log exactly (arrival, trailer and DO# on all three).
2. **A load hooked with no destination vanished.** "Load trailer pickup sds
   dock28 #77209" -- the driver never said where it was going, so nothing was
   recorded and nothing was said. Now raises **MANUAL RECONCILE: Load Picked Up
   With No Destination**, from both the yard-move and the arrival paths, and
   still writes no leg (a leg to UNKNOWN enters a round and a route as if real).
   Dispatcher's call: they read the destination off the later arrival.
3. **"Finish live unloading at pactra #3 move to Dock 47" became a leg to
   UNKNOWN.** A door read as travel. Prompt now states a door is never a
   destination (verified against the live parser); CASE 3 resolves each door to
   its building via `docks.csv` and stamps the completion on the trip that
   ended there -- and only if it ended *there*, or the unload lands on the
   wrong leg.
4. **`replay_scratch` now says where it is** at the end of a run. It was always
   kept (dropped only at the start of the next run); nothing was deleting it.

New: `data/docks.csv` + `location_docks` table + `ai_engine.facility_for_dock`
/ `dock_use`. `dock_use` is recorded and **not yet acted on** -- it is what
would later let load type be read off the door rather than guessed.

Replay 08/28 went 86/120 → 88/120 matched (72%→73%). That number only compares
origin, destination and *departure* time, so it does not see the arrival times,
DO numbers and trailers these fixes corrected.

## Done 2026-08-30 (later, uncommitted)

Two cases the dispatcher reported from 08/28, both traced to the same shape of
defect: a signal that was already in the message went unread.

1. **The paperwork says where the load is going.** "Load trailer pickup sds
   dock28 #77209" captioned a BOL consigned to 200 Momeni Lane. The vision pass
   never read SHIP TO at all, so the run was lost. Vision now extracts
   `ship_to_address`, `ai_engine.facility_for_address` resolves it against the
   `address` column of `location_codes` (house number + street only -- the
   suffix drifts between the BOL's print and locations.csv), and it fills the
   destination **only** when the driver named none. Skipped for a duplicate
   BOL, and skipped when the consignee is the site they are standing on.
   Falls back to the existing card when it cannot resolve.
2. **That message reached no handler at all.** The parser files it
   `CASE_2_DESTINATION_ARRIVAL` with SDS as the *destination* -- "pickup sds
   dock28" reads as travel TO sds. With a trip open to SDS the arrival branch
   stamped the arrival and returned clean, and the hook was never seen; with no
   open trip, the orphan-pickup card tested `origin_loc` only, which was
   UNKNOWN. Both now route through `start_hooked_load`.
3. **A delivered load left the yard booked LOADED.** "Finish live unloading at
   pactra #10 heading to sds" with a receiver-stamped BOL. The branch meant to
   catch this tested `action_type` for `UNLOAD_COMPLETED`/`FINISHED_UNLOAD`,
   which **the parser cannot emit** -- its vocabulary is `LIVE_UNLOAD` /
   `DROP_DOCK` / `DROP_YARD` / `DROP_DOOR` / `BOBTAIL_ARRIVE`. It never fired,
   so load status fell through to inheriting the previous leg's. Replaced with
   `just_delivered` (receiver stamp, or work_finished on an unload) and its
   mirror `just_loaded` -- the `LIVE_LOAD`/`HOOK` branch was equally dead, so
   "live loading finished load 200 to E2F" was departing EMPTY.
4. **Receivers stamp now, they do not sign.** The vision prompt explicitly said
   a company stamp is NOT a receiver signature, so every stamped POD read as
   missing. It now turns on WHICH BLOCK the mark sits in, not on handwriting.
5. **`do_number` was capturing the printed "Do No." field** -- the shipper's
   own reference, which looks like the BOL number -- instead of the SDS clerk's
   hand-written yard slot. Prompt now requires handwriting and digits only.
6. `compress_image` raised on any RGBA PNG ("cannot write mode RGBA as JPEG")
   and silently sent the full-size original. Screenshots are RGBA.

`CASE 1`'s departure INSERT is now `open_departure_leg`, shared with the hooked
-load paths rather than copied.

**Verified against the two real photos** (`extract_bol_locally` on the
dispatcher's screenshots): ship-to resolves to 200F on both, the stamp reads as
`receiver_signed`, and `do_number` is `33` on one and null on the other.

Not yet checked: whether the CASE 1 **auto-heal** swallows a POD posted
alongside an onward departure. It claims any open LOADED leg missing a BOL, and
would then never reach the departure. It did not fire on 08/28 because the
inbound leg already had its paperwork.

## Done 2026-09-01 (uncommitted, working tree)

**A CASE 3 dock-move with `work_finished` stamped the completion but never
closed the trip leg.** Replay of 윤석환 (SOKHWAN YUN, 6619801377) with
`REPLAY_PHOTOS=1` produced a 9th leg for the 15:28:40 message "Finish live
unloading at pactra #3  move to Dock 47": the SDS→200F leg (14:24, the day's
last inbound run) stayed `UNLOADING` with `finished_time=15:28:40` stamped,
while a new `DOCK_MOVE` positioning leg (dock 3 → dock 47, 200F→200R) was
inserted. The dispatcher's log records only the 8 legs; the unload finished at
15:28 and the dock move is repositioning, not a trip.

`stamp_finished()` only wrote `finished_time`; the `leg_status` update lived in
`finish_rm_load` alone, which the CASE 3 work_finished path never reached.
Now `stamp_finished` also sets `leg_status='COMPLETED'` -- it is the single
seam shared by `CASE_WORK_FINISHED`, the merged CASE 1 departure, the CASE 3
drop-is-arrival path and the CASE 3 work_finished path, so all four close the
leg they end. Test `test_dock_move_across_the_site_records_both_buildings`
extended to assert the delivery leg ends COMPLETED. Replay rerun with the fix:
leg 8 now COMPLETED (arrival 15:02:12, finished 15:28:40), leg 9 kept as the
positioning move 200F→200R dock 3→47.

Note for future replays: `docker compose ... run` foreground runs hang the
terminal exec; use `docker compose ... run -d --name ...` and poll
`docker inspect` for State.ExitCode (or docker wait) -- the container lives
under the daemon, which survives across tool calls, and `--rm` cleans it up.

**Round numbers were all 1 and `trip_seq` was a stray second column.** The
round counters (`resolve_round` last-leg lookup, `open_new_round` MAX,
`current_round_number`) filtered on `CURRENT_DATE()`, which never matched the
replay's past-day legs, so every leg got round 1. All round queries now scope
to the departure's own day (`DATE(departure_time) = DATE(msg_timestamp)`).
The duplicate `trip_seq` column is dropped from the DDL, the additive list
and, idempotently, from existing databases via `apply_schema`. Photos-on
replay now yields rounds 1,1/2,2/3,3/4,4 for SOKHWAN YUN -- matching the
dispatcher's trip seq 0,1,2,3,4 (the bot counts from 1 and folds the empty
repositioning leg into its round).

**Load types were NULL on every leg.** `bot/data/load_types.csv` is keyed by
exact facility pair (`200F,E2F`) because E2R→200R = RM Inbound while E2R→200F
= FG Inbound differ at the door, but `classify()` looked the lane up by site
pair (`200,E2`), which no key matched. It now tries the exact facility lane
first, then a site-pair fallback that is only used when it maps to a single
type (a mixed pair like (E2,200) stays untyped). Empty-only lanes were pruned
from the CSV (`7634→200`, `7634→SDS`, `210→E1`, the `3551` reverse rows), and
the repo-root `data/load_types.csv` was synced to match `bot/data/` so the two
copies cannot diverge again.

## Things I got wrong (do not repeat)

- Asserted `gemini-3.5-flash-lite` did not exist. It does.
- Mapped PACTRA to 7634 from its official name. It is 200.
- Read a manifest row `11:44→12:58` as an OCR error. It was a lunch break.
- Built a lunch heuristic before learning drivers report lunch in chat.
- Told the parser drivers "rarely name the facility" on internal moves, so it
  put `200R` into a dock field.
- Guessed at `E2F→E2R` as a wrong-destination example; it is one site.
- Wrote `docks.csv` to the root `data/` and watched it seed nothing. The bot
  reads `bot/data/`.
- Guarded the drop-is-an-arrival rule on the leg's trailer matching, which
  blocked two of the three real arrivals it was meant to fix. The stored
  trailer was itself a guess.

## Open items

- **`load_types.csv` dual-copy trap is closed.** `bot/data/load_types.csv` now
  holds the curated lanes (the copy the code reads, with empty-only moves
  pruned), `classify()` matches it, and the repo-root `data/load_types.csv`
  was copied to match. The two-copy hazard remains for the other CSVs --
  edit `bot/data/` or the change does nothing.
- **Two drivers produce no legs at all:** DOMINIQUE WALLS and RODERICK MCBRIDE,
  14 manual rows between them on 08/28, every message skipped. Looks like a
  roster gap (`/roster`, `drivers.csv`), not parsing. Biggest single win left
  in the replay.
- **Departures that never name a destination** are now read off the BOL's
  SHIP TO when a photo is attached, and carded as before when it is not.
  Nothing yet infers a destination from the arrival that follows.
- **A same-site departure is only detected before origin inference.** "Lunch
  break off heading to 200" with no origin became a real `200F→200F` leg,
  because the same-site check at the top of `commit_trip_leg` runs while origin
  is still UNKNOWN. Matthew Cho 13:46 on 08/28.
- **"Hong il pyo pick up Load SDS to 200" parsed as SDS→SDS.** A clean message
  the model got wrong; worth a bench case.
- Not deployed. `docker compose up -d --build` (`--build` matters: deps changed).
- **RDS** deferred; DB was wiped once by a t2→t3 resize. Nightly `mysqldump`
  service exists as a stopgap.
- Re-run the replay after any parser or CASE 3 change; it is the only check
  that sees a whole day.
- Manifest reconciliation report (rows are stored and validated, not yet matched
  against legs).
- Dashboard, once the data settles.
- Dwell thresholds should be re-derived from real data after 2-3 weeks.

## On first deploy, expect

POD alerts, parse-failure cards and dwell alerts fire **for the first time
ever** — a burst reflecting accumulated habit, not a regression. Drivers will
start getting clock-in acknowledgements and an occasional gate-report nudge
(max once a week each). Tell them before it starts talking. Check `/roster` on
day one: anyone unrecognised is being silently ignored.
